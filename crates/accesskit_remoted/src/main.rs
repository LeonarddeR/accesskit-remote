//! The Linux-side daemon: wires the AT-SPI tree source to the server core and
//! a socket transport. Runs as a systemd user service in the (WSL) session.

fn main() {
    eprintln!("accesskit_remoted: not implemented yet");
}
